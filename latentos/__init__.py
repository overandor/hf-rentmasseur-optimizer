"""LatentOS + LatentOS EDU — Credential-Settled Latent Liquidity Network.

LatentOS liquidifies pre-market software value.
LatentOS EDU liquidifies verified learning progress.

Together: any valuable but illiquid progress signal becomes financeable
only when transformed into a minimally sufficient, verifiable, permissioned receipt.

    unstructured progress
    -> truth-graded claim
    -> issuer / evidence / provenance
    -> risk score
    -> liquidity cap
    -> endpoint opportunity
    -> settlement route

Doctrine: liquidify the proof, not the person; liquidify the artifact, not the private source record.
"""

__version__ = "1.0.0"
